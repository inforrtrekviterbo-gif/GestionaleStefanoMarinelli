# Redesign schermata Cassa — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rifare grafica e UX della schermata Cassa (layout "carrello protagonista", carrello leggibile con prezzo bloccato e sconto €/%, scanner globale, cliente inline, menu Operazioni) e aggiungere un catalogo servizi vendibili in cassa con pagina admin.

**Architecture:** La cassa è un unico grande componente client `NewCashRegister` in `app/cash-register.tsx`. Il redesign estrae sotto-componenti focalizzati (`CartRow`, `CustomerInline`, `OperationsMenu`, `CashSearch`) e un hook `useGlobalScanner`, e ricompone il render. I servizi sono una nuova tabella Postgres esposta nel bootstrap dati e gestita da una pagina admin in `app/gestionale.tsx`. La logica di vendita/fiscale/magazzino non cambia.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript, Tailwind v4 + `app/globals.css`, Postgres via shim `lib/db.ts`, API in `app/api/data/route.ts`.

**Verifica (niente test runner nel repo):** ogni task si valida con `npx tsc --noEmit` + `npx next build` (0 errori) e un check manuale descritto. Usare `/opt/homebrew/bin/git` per i commit (Xcode blocca `/usr/bin/git`). Committare a fine di ogni task.

**Spec di riferimento:** `docs/superpowers/specs/2026-09-17-cassa-redesign-design.md`

---

## File coinvolti (mappa)

- `lib/runtime-db.ts` — `schemaStatements`: aggiunta tabella `services`.
- `supabase/migrations/0002_services.sql` — nuova migrazione (crea `services`).
- `app/api/data/route.ts` — `bootstrap()` include `services`; nuove action `createService`/`updateService`/`deleteService`; `createCustomer` ritorna il cliente creato.
- `app/gestionale.tsx` — tipo `Bootstrap` con `services`; voce menu admin "Servizi"; componente `Services` (pagina gestione).
- `app/cash-register.tsx` — tipo `CashData` con `services`; nuovo hook `useGlobalScanner`; componenti `CartRow`, `CustomerInline`, `OperationsMenu`, `CashSearch`; render ricomposto.
- `app/globals.css` — stili: barra scan, carrello nuovo, stepper, sconto toggle, chip cliente, menu operazioni, lista risultati, pagina servizi.

---

## Task 1: Tabella `services` (schema + migrazione)

**Files:**
- Modify: `lib/runtime-db.ts` (array `schemaStatements`, inizia a riga ~40)
- Create: `supabase/migrations/0002_services.sql`

- [ ] **Step 1: Aggiungere la CREATE TABLE a `schemaStatements`**

In `lib/runtime-db.ts`, dentro l'array `schemaStatements`, subito dopo la riga di `catalog_products` (`CREATE TABLE IF NOT EXISTS catalog_products ...`), aggiungere:

```ts
`CREATE TABLE IF NOT EXISTS services (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name TEXT NOT NULL, price double precision NOT NULL DEFAULT 0, active INTEGER NOT NULL DEFAULT 1, created_at TEXT NOT NULL)`,
```

- [ ] **Step 2: Creare la migrazione Postgres**

Create `supabase/migrations/0002_services.sql`:

```sql
-- Catalogo servizi vendibili in cassa (nessuna giacenza)
CREATE TABLE IF NOT EXISTS services (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT NOT NULL,
  price double precision NOT NULL DEFAULT 0,
  active INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL
);
```

- [ ] **Step 3: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori. Il dev DB (pglite) crea la tabella allo startup tramite `schemaStatements`.

- [ ] **Step 4: Verifica manuale (dev)**

Run: `npm run dev`, fare login admin. Nessun errore in console all'avvio (ensureDatabase esegue `schemaStatements`).

- [ ] **Step 5: Commit**

```bash
/opt/homebrew/bin/git add lib/runtime-db.ts supabase/migrations/0002_services.sql
/opt/homebrew/bin/git commit -m "Servizi: tabella services (schema + migrazione)"
```

---

## Task 2: API servizi (bootstrap + CRUD) e createCustomer con ritorno

**Files:**
- Modify: `app/api/data/route.ts` (`bootstrap()` ~riga 172-193; router action ~riga 1111; `createCustomer` ~riga 302)

- [ ] **Step 1: Leggere i servizi nel bootstrap**

In `app/api/data/route.ts`, dentro `bootstrap()`, prima del `return`, aggiungere:

```ts
  const services = await all<{ id: number; name: string; price: number; active: number }>(
    `SELECT id, name, price, active FROM services WHERE active = 1 ORDER BY name`);
```

Poi aggiungere `services` all'oggetto ritornato:

```ts
  return { user, products, customers, sales, gifts, reservations, transfers, documents, saleItems, fiscalDevices, fiscalJobs, services, generatedAt: new Date().toISOString() };
```

- [ ] **Step 2: Action CRUD servizi (solo admin)**

In `app/api/data/route.ts`, vicino alle altre funzioni action (es. dopo `createCustomer`, ~riga 340), aggiungere:

```ts
async function createService(user: SessionUser, body: JsonMap) {
  if (user.role !== "admin") throw new Error("Solo l'amministratore può gestire i servizi.");
  const name = String(body.name ?? "").trim();
  const price = Math.max(0, Number(body.price) || 0);
  if (!name) throw new Error("Nome servizio obbligatorio.");
  await database().prepare(`INSERT INTO services (name, price, active, created_at) VALUES (?, ?, 1, ?)`)
    .bind(name, price, new Date().toISOString()).run();
  await logActivity(user, "createService", `Servizio creato: ${name}`);
  return { ok: true };
}

async function updateService(user: SessionUser, body: JsonMap) {
  if (user.role !== "admin") throw new Error("Solo l'amministratore può gestire i servizi.");
  const id = Number(body.id);
  const name = String(body.name ?? "").trim();
  const price = Math.max(0, Number(body.price) || 0);
  const active = body.active === false || body.active === 0 ? 0 : 1;
  if (!id || !name) throw new Error("Dati servizio non validi.");
  await database().prepare(`UPDATE services SET name = ?, price = ?, active = ? WHERE id = ?`)
    .bind(name, price, active, id).run();
  await logActivity(user, "updateService", `Servizio aggiornato: ${name}`);
  return { ok: true };
}

async function deleteService(user: SessionUser, body: JsonMap) {
  if (user.role !== "admin") throw new Error("Solo l'amministratore può gestire i servizi.");
  const id = Number(body.id);
  if (!id) throw new Error("Servizio non valido.");
  await database().prepare(`DELETE FROM services WHERE id = ?`).bind(id).run();
  await logActivity(user, "deleteService", `Servizio eliminato #${id}`);
  return { ok: true };
}
```

> Nota: verificare la firma reale di `logActivity` nel file e adeguare i parametri (usare lo stesso pattern delle action vicine, es. `createCustomer`). Se le mutazioni sono protette dal test-mode guard, seguire lo stesso schema delle altre action.

- [ ] **Step 3: Registrare le action nel router**

Dove sono mappate le action (es. `if (action === "createCustomer") return createCustomer(auth.user, body);`, ~riga 1111), aggiungere:

```ts
  if (action === "createService") return createService(auth.user, body);
  if (action === "updateService") return updateService(auth.user, body);
  if (action === "deleteService") return deleteService(auth.user, body);
```

- [ ] **Step 4: `createCustomer` ritorna il cliente creato**

In `createCustomer` (~riga 302), dopo l'INSERT, recuperare e ritornare la riga (serve per l'associazione inline in cassa). Sostituire il `return` finale con:

```ts
  const created = await database().prepare(
    `SELECT id, customer_type AS customerType, first_name AS firstName, last_name AS lastName, company_name AS companyName, vat_number AS vatNumber, pec, sdi_code AS sdiCode, phone, email, address, postal_code AS postalCode, city, province, tax_code AS taxCode, scope, created_store AS createdStore, created_at AS createdAt FROM customers WHERE id = ?`
  ).bind(lastId).first();
  return { ok: true, customer: created };
```

> `lastId`: usare l'id restituito dall'INSERT come già fatto altrove nel file (il shim espone `last_row_id`/`RETURNING id`). Adeguare al pattern esistente nella funzione (se `createCustomer` non cattura l'id, aggiungere `const info = await ...insert...run(); const lastId = info.meta.last_row_id;` secondo il pattern del file).

- [ ] **Step 5: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 6: Verifica manuale**

`npm run dev`, login admin. In console del browser: `fetch('/api/data',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'createService',name:'Sciolinatura',price:15})}).then(r=>r.json()).then(console.log)` → `{ok:true}`. Poi `GET /api/data` mostra `services` con la voce.

- [ ] **Step 7: Commit**

```bash
/opt/homebrew/bin/git add app/api/data/route.ts
/opt/homebrew/bin/git commit -m "Servizi: API CRUD + bootstrap; createCustomer ritorna il cliente"
```

---

## Task 3: Pagina admin "Servizi"

**Files:**
- Modify: `app/gestionale.tsx` (tipo `Bootstrap` ~riga 25; `adminMenuGroups` ~riga 973; `content` map ~riga 1081; nuovo componente `Services`)
- Modify: `app/globals.css` (stili pagina servizi, riuso pattern esistenti)

- [ ] **Step 1: Aggiungere `services` al tipo `Bootstrap`**

In `app/gestionale.tsx`, nel tipo `Bootstrap` (riga 25), aggiungere il campo:

```ts
services: { id: number; name: string; price: number; active: number }[];
```

- [ ] **Step 2: Componente `Services`**

Aggiungere in `app/gestionale.tsx` (vicino agli altri componenti pagina, es. prima di `adminMenuGroups`):

```tsx
function Services({ data, reload }: { data: Bootstrap; reload: () => Promise<void> }) {
  const [name, setName] = useState("");
  const [price, setPrice] = useState("");
  const [editing, setEditing] = useState<{ id: number; name: string; price: string } | null>(null);
  const [error, setError] = useState("");
  const services = data.services ?? [];
  async function add(event: React.FormEvent) {
    event.preventDefault(); setError("");
    if (isClientTestMode()) { setError("Modalità TEST attiva: servizio non salvato."); return; }
    try { await post("createService", { name, price: Number(price) || 0 }); setName(""); setPrice(""); await reload(); }
    catch (reason) { setError(reason instanceof Error ? reason.message : "Errore."); }
  }
  async function saveEdit() {
    if (!editing) return; setError("");
    try { await post("updateService", { id: editing.id, name: editing.name, price: Number(editing.price) || 0 }); setEditing(null); await reload(); }
    catch (reason) { setError(reason instanceof Error ? reason.message : "Errore."); }
  }
  async function remove(id: number, label: string) {
    if (!confirm(`Eliminare il servizio "${label}"?`)) return;
    try { await post("deleteService", { id }); await reload(); }
    catch (reason) { setError(reason instanceof Error ? reason.message : "Errore."); }
  }
  return <section className="screen"><div className="screen-head"><div><p className="eyebrow">ANAGRAFICHE</p><h1>Servizi</h1><p className="muted">Servizi vendibili in cassa (nome e prezzo). Compaiono nella ricerca della cassa.</p></div></div>
    <div className="panel"><form className="form-grid" onSubmit={add}><ClearableInput label="Nome servizio" value={name} onChange={setName} required /><ClearableInput label="Prezzo" type="number" min="0" step="0.01" value={price} onChange={setPrice} required /><div className="form-actions align-end"><button className="primary">Aggiungi servizio</button></div></form>{error && <div className="alert danger">{error}</div>}</div>
    <div className="panel"><div className="table-wrap"><table><thead><tr><th>Servizio</th><th>Prezzo</th><th>Azioni</th></tr></thead><tbody>{!services.length ? <tr><td colSpan={3}><Empty>Nessun servizio. Aggiungine uno qui sopra.</Empty></td></tr> : services.map((s) => <tr key={s.id}>{editing?.id === s.id ? <><td><ClearableInput compact value={editing.name} onChange={(v) => setEditing({ ...editing, name: v })} /></td><td><ClearableInput compact type="number" min="0" step="0.01" value={editing.price} onChange={(v) => setEditing({ ...editing, price: v })} /></td><td><div className="table-actions"><button className="primary small" onClick={saveEdit}>Salva</button><button className="secondary small" onClick={() => setEditing(null)}>Annulla</button></div></td></> : <><td><strong>{s.name}</strong></td><td>{money(s.price)}</td><td><div className="table-actions"><button className="secondary small" onClick={() => setEditing({ id: s.id, name: s.name, price: String(s.price) })}>Modifica</button><button className="text-button negative" onClick={() => remove(s.id, s.name)}>Elimina</button></div></td></>}</tr>)}</tbody></table></div></div>
  </section>;
}
```

> `isClientTestMode`, `post`, `ClearableInput`, `Empty`, `money`, `MaterialIcon` sono già definiti/usati in `app/gestionale.tsx`; verificarne il nome esatto e riusarli.

- [ ] **Step 3: Voce di menu admin + routing pagina**

In `adminMenuGroups` (riga 973), nel gruppo "Anagrafiche", aggiungere la voce dopo "Prenotazioni e acconti":

```ts
["services", "handyman", "Servizi"],
```

In `content` (riga 1081), aggiungere:

```ts
services_page: <Services data={data} reload={reload} />,
```

> Attenzione ai nomi chiave: la chiave menu è `services`, ma nel bootstrap esiste già il campo dati `services`. Per evitare confusione usare come chiave di pagina `services` nel menu e mapparla nel `content` con la stessa stringa. Verificare che `content[page]` usi la chiave del menu: se la tupla menu usa `"services"`, la voce in `content` deve essere `services: <Services .../>`. Allineare le due stringhe (usare `services`).

Correzione: usare la **stessa** chiave. Menu: `["services", "handyman", "Servizi"]`. Content: `services: <Services data={data} reload={reload} />,`.

- [ ] **Step 4: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 5: Verifica manuale**

`npm run dev`, login admin → menu Anagrafiche mostra "Servizi". Aggiungere "Sciolinatura 15€", appare in tabella; Modifica e Elimina funzionano. In modalità TEST non salva.

- [ ] **Step 6: Commit**

```bash
/opt/homebrew/bin/git add app/gestionale.tsx app/globals.css
/opt/homebrew/bin/git commit -m "Servizi: pagina admin di gestione"
```

---

## Task 4: Hook scanner globale

**Files:**
- Modify: `app/cash-register.tsx` (aggiungere hook e usarlo in `NewCashRegister`)

- [ ] **Step 1: Aggiungere l'hook `useGlobalScanner`**

In `app/cash-register.tsx`, a livello modulo (vicino agli altri helper, dopo `Scanner`), aggiungere:

```ts
// Riconosce lo "sparo" dello scanner (tasti velocissimi + Enter) da qualunque
// punto della pagina. La digitazione lenta a mano non viene intercettata.
function useGlobalScanner(onScan: (code: string) => void) {
  useEffect(() => {
    let buffer = "";
    let lastTime = 0;
    const MAX_GAP = 50; // ms tra due tasti dello scanner
    const MIN_LEN = 6;  // lunghezza minima per considerarlo un codice
    function onKeyDown(event: KeyboardEvent) {
      const now = event.timeStamp;
      if (now - lastTime > MAX_GAP) buffer = "";
      lastTime = now;
      if (event.key === "Enter") {
        const code = buffer;
        buffer = "";
        if (code.length >= MIN_LEN) { event.preventDefault(); onScan(code); }
        return;
      }
      if (event.key.length === 1) buffer += event.key;
    }
    window.addEventListener("keydown", onKeyDown, true);
    return () => window.removeEventListener("keydown", onKeyDown, true);
  }, [onScan]);
}
```

- [ ] **Step 2: Usare l'hook in `NewCashRegister`**

Dentro `NewCashRegister`, dopo la definizione di `scan`, aggiungere una callback stabile e collegarla:

```ts
  const scanRef = useRef(scan);
  scanRef.current = scan;
  const onGlobalScan = useCallback((code: string) => { void scanRef.current(code); }, []);
  useGlobalScanner(onGlobalScan);
```

Aggiungere `useCallback` all'import da `react` in cima al file (riga 3).

> Motivo del `scanRef`: `scan` è ridefinita a ogni render (usa `total`, `cart`, ecc.); l'hook resta stabile e chiama sempre l'ultima versione.

- [ ] **Step 3: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 4: Verifica manuale**

`npm run dev`, cassa. Con il focus **fuori** dal campo EAN (es. click su area vuota), incollare/digitare velocemente un EAN valido + Invio → il prodotto entra in carrello. Digitando **lentamente** in un campo (es. ricerca) i tasti restano nel campo e non scatta la scansione.

- [ ] **Step 5: Commit**

```bash
/opt/homebrew/bin/git add app/cash-register.tsx
/opt/homebrew/bin/git commit -m "Cassa: scanner globale (lettura da tutta la pagina)"
```

---

## Task 5: Riga carrello ridisegnata (CartRow) con prezzo bloccato e sconto €/%

**Files:**
- Modify: `app/cash-register.tsx` (nuovo componente `CartRow`; helper conversione sconto; uso nel render carrello)
- Modify: `app/globals.css` (stili riga carrello, stepper, sconto toggle)

- [ ] **Step 1: Helper conversione sconto € → %**

In `app/cash-register.tsx`, a livello modulo (vicino a `lineTotal`), aggiungere:

```ts
// Converte uno sconto in euro (sull'imponibile riga) in percentuale 0..100.
function euroDiscountToPercent(quantity: number, unitPrice: number, euro: number) {
  const base = quantity * unitPrice;
  if (base <= 0) return 0;
  return Math.min(100, Math.max(0, Math.round((euro / base) * 10000) / 100));
}
// Percentuale -> euro scontati (per mostrare l'importo quando la modalità è €).
function percentToEuroDiscount(quantity: number, unitPrice: number, percent: number) {
  return Math.round(quantity * unitPrice * (percent / 100) * 100) / 100;
}
```

- [ ] **Step 2: Componente `CartRow`**

Aggiungere in `app/cash-register.tsx`:

```tsx
function CartRow({ item, product, onQty, onDiscountPercent, onRemove }: {
  item: CartItem;
  product: Product | undefined;
  onQty: (quantity: number) => void;
  onDiscountPercent: (percent: number) => void;
  onRemove: () => void;
}) {
  const [mode, setMode] = useState<"pct" | "eur">("pct");
  const editable = !item.locked && item.quantity > 0 && !["gift", "deposit", "repair_deposit", "return", "reservation_balance"].includes(item.itemType);
  const discountValue = mode === "pct" ? item.discountPercent : percentToEuroDiscount(item.quantity, item.unitPrice, item.discountPercent);
  function applyDiscount(raw: string) {
    const num = Math.max(0, Number(raw) || 0);
    onDiscountPercent(mode === "pct" ? Math.min(100, num) : euroDiscountToPercent(item.quantity, item.unitPrice, num));
  }
  return <div className={`cart-line ${item.itemType === "return" ? "return-row" : ""}`}>
    <ProductThumb photoKey={product?.photoKey} name={item.description} className="cart-line-thumb" />
    <div className="cart-line-main">
      <strong className="cart-line-name">{item.description}</strong>
      <small className="cart-line-sub">{money(item.unitPrice)} cad.{item.itemType !== "product" ? ` · ${item.itemType.replaceAll("_", " ")}` : ""}</small>
    </div>
    {editable ? <>
      <div className="qty-stepper"><button type="button" onClick={() => onQty(Math.max(1, item.quantity - 1))} aria-label="Diminuisci">−</button><input type="number" min="1" step="1" value={item.quantity} onChange={(e) => onQty(Math.max(0, Number(e.target.value) || 0))} aria-label="Quantità" /><button type="button" onClick={() => onQty(item.quantity + 1)} aria-label="Aumenta">+</button></div>
      <div className="disc-field"><input type="number" min="0" step="0.01" value={discountValue} onChange={(e) => applyDiscount(e.target.value)} aria-label="Sconto" /><button type="button" className="disc-toggle" onClick={() => setMode((m) => m === "pct" ? "eur" : "pct")} aria-label="Cambia unità sconto">{mode === "pct" ? "%" : "€"}</button></div>
    </> : <><span className="cart-line-locked">×{item.quantity}</span><span /></>}
    <b className="cart-line-total">{money(lineTotal(item))}</b>
    <button type="button" className="cart-line-remove" onClick={onRemove} aria-label="Rimuovi"><MaterialIcon>delete</MaterialIcon></button>
  </div>;
}
```

> `ProductThumb` è usato altrove nella cassa (griglia prodotti). Verificarne l'import/definizione e riusarlo; se è definito in `gestionale.tsx` e non importabile, replicare il piccolo componente thumb già presente in `cash-register.tsx` o passare `photoKey`/iniziali.

- [ ] **Step 3: Usare `CartRow` nel render del carrello**

Sostituire l'attuale `.cart-list` (riga ~656, il blocco `cart.map(...)` con `cart-row cash-cart-row`) con:

```tsx
<div className="cart-list">{cart.map((item) => {
  const product = item.productId != null ? data.products.find((p) => p.id === item.productId) : undefined;
  return <CartRow key={item.key} item={item} product={product}
    onQty={(quantity) => updateItem(item.key, { quantity })}
    onDiscountPercent={(percent) => updateItem(item.key, { discountPercent: percent })}
    onRemove={() => removeItem(item.key)} />;
})}</div>
```

Mantenere il rendering dei `cartDetailLines` (dettaglio prenotazione/risuolatura) sotto la riga se presente: se serve, spostarlo dentro `CartRow` passando `details` come prop, oppure lasciarlo come blocco separato subito dopo. Per semplicità in questo task: se `cartDetailLines(item).length > 0`, renderizzare il blocco dettaglio esistente subito dopo `<CartRow/>` nello stesso `.map`.

Rimuovere l'header a colonne `.cart-header` (Prezzo/Sconto/Totale modificabile): non serve più con le righe a card.

- [ ] **Step 4: CSS riga carrello**

In `app/globals.css` aggiungere:

```css
/* Cassa: righe carrello a card leggibili */
.cart-line { display: flex; align-items: center; gap: .8rem; padding: .7rem .9rem; border: 1px solid rgba(66,199,255,.14); border-radius: 14px; background: var(--panel); margin-bottom: .55rem; }
.cart-line.return-row { border-color: rgba(255,138,156,.4); }
.cart-line-thumb { width: 44px; height: 44px; border-radius: 10px; flex-shrink: 0; }
.cart-line-main { flex: 1; min-width: 0; }
.cart-line-name { display: block; font-weight: 750; }
.cart-line-sub { color: var(--muted, #8aa0b5); font-size: .78rem; }
.qty-stepper { display: inline-flex; align-items: center; border: 1px solid rgba(66,199,255,.3); border-radius: 10px; overflow: hidden; }
.qty-stepper button { width: 36px; height: 36px; border: 0; background: rgba(66,199,255,.08); color: inherit; font-size: 1.1rem; font-weight: 800; cursor: pointer; }
.qty-stepper input { width: 46px; height: 36px; border: 0; background: transparent; color: inherit; text-align: center; font-weight: 700; }
.disc-field { display: inline-flex; align-items: center; border: 1px solid rgba(66,199,255,.3); border-radius: 10px; overflow: hidden; }
.disc-field input { width: 62px; height: 36px; border: 0; background: transparent; color: inherit; text-align: right; padding: 0 .35rem; }
.disc-toggle { width: 34px; height: 36px; border: 0; background: rgba(66,199,255,.12); color: inherit; font-weight: 800; cursor: pointer; }
.cart-line-total { min-width: 84px; text-align: right; font-weight: 850; font-size: 1.05rem; }
.cart-line-remove { width: 36px; height: 36px; border-radius: 10px; border: 0; background: rgba(255,138,156,.14); color: #ff8a9c; display: grid; place-items: center; cursor: pointer; flex-shrink: 0; }
.cart-line-locked { min-width: 92px; text-align: center; color: var(--muted, #8aa0b5); font-weight: 700; }
@media (max-width: 720px) { .cart-line { flex-wrap: wrap; } .cart-line-total { min-width: auto; } }
```

- [ ] **Step 5: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 6: Verifica manuale**

Cassa: aggiungere 2 prodotti. Le righe sono leggibili; il **prezzo unitario non è modificabile**; lo stepper `− +` cambia la quantità; il campo Sconto cambia con toggle **%↔€** (in € l'importo si converte in sconto corretto); il totale riga si aggiorna. Righe reso/saldo restano bloccate.

- [ ] **Step 7: Commit**

```bash
/opt/homebrew/bin/git add app/cash-register.tsx app/globals.css
/opt/homebrew/bin/git commit -m "Cassa: riga carrello ridisegnata (prezzo bloccato, stepper, sconto €/%)"
```

---

## Task 6: Ricerca unificata prodotti + servizi (griglia solo cercando)

**Files:**
- Modify: `app/cash-register.tsx` (`CashData` + componente `CashSearch`; uso nel render; `addService`)
- Modify: `app/globals.css` (lista risultati compatta)

- [ ] **Step 1: Aggiungere `services` a `CashData`**

In `app/cash-register.tsx`, nel tipo `CashData` (riga 29), aggiungere:

```ts
services: { id: number; name: string; price: number; active: number }[];
```

- [ ] **Step 2: Funzione per aggiungere un servizio al carrello**

Dentro `NewCashRegister`, vicino a `addProduct`, aggiungere:

```ts
  function addService(service: { id: number; name: string; price: number }) {
    setCart((current) => [...current, { key: keyId(), productId: null, description: service.name, quantity: 1, unitPrice: service.price, discountPercent: 0, itemType: "service", metadata: { serviceId: service.id } }]);
    setTotalOverride(""); setError(""); setNotice(`${service.name} aggiunto al carrello.`);
  }
```

- [ ] **Step 3: Componente `CashSearch`**

Aggiungere in `app/cash-register.tsx`:

```tsx
function CashSearch({ products, services, available, onProduct, onService }: {
  products: Product[];
  services: { id: number; name: string; price: number; active: number }[];
  available: (product: Product) => number;
  onProduct: (product: Product) => void;
  onService: (service: { id: number; name: string; price: number }) => void;
}) {
  const [query, setQuery] = useState("");
  const q = query.trim().toLocaleLowerCase("it");
  const prodMatches = q ? products.filter((p) => available(p) > 0 && `${p.brand} ${p.name} ${p.color} ${p.size} ${p.sku} ${p.eans}`.toLocaleLowerCase("it").includes(q)).slice(0, 40) : [];
  const svcMatches = q ? (services ?? []).filter((s) => s.active && s.name.toLocaleLowerCase("it").includes(q)).slice(0, 20) : [];
  return <div className="cash-search">
    <div className="search-hero"><MaterialIcon>search</MaterialIcon><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Cerca prodotto o servizio per nome, colore, taglia, SKU…" /></div>
    {q && <div className="cash-search-results">
      {!prodMatches.length && !svcMatches.length ? <Empty>Nessun risultato.</Empty> : <>
        {prodMatches.map((p) => <button type="button" className="cash-result" key={`p-${p.id}`} onClick={() => { onProduct(p); setQuery(""); }}><span><strong>{p.name}</strong><small>{p.brand} · {p.color} · {p.size} · {available(p)} pz</small></span><b>{money(p.price)}</b></button>)}
        {svcMatches.map((s) => <button type="button" className="cash-result service" key={`s-${s.id}`} onClick={() => { onService(s); setQuery(""); }}><span><strong>{s.name}</strong><small>Servizio</small></span><b>{money(s.price)}</b></button>)}
      </>}
    </div>}
  </div>;
}
```

- [ ] **Step 4: Sostituire la vecchia griglia sempre visibile**

Nel render, rimuovere il blocco `scanner-panel` che mappa `data.products` in `.cash-product-grid` (riga ~649-653) e usare al suo posto lo scanner + `CashSearch`:

```tsx
<div className="panel scanner-panel">
  <div className="scanner-panel-head"><p className="eyebrow">SCANNER</p><Scanner onScan={scan} /></div>
  <CashSearch products={data.products} services={data.services} available={available} onProduct={addProduct} onService={addService} />
</div>
```

- [ ] **Step 5: CSS lista risultati**

In `app/globals.css`:

```css
/* Cassa: risultati ricerca compatti */
.cash-search-results { margin-top: .6rem; display: flex; flex-direction: column; gap: .3rem; max-height: 320px; overflow: auto; }
.cash-result { display: flex; align-items: center; justify-content: space-between; gap: .8rem; padding: .55rem .75rem; border: 1px solid rgba(66,199,255,.14); border-radius: 10px; background: var(--panel); color: inherit; cursor: pointer; text-align: left; }
.cash-result:hover { background: rgba(66,199,255,.1); }
.cash-result span { display: flex; flex-direction: column; min-width: 0; }
.cash-result small { color: var(--muted, #8aa0b5); font-size: .74rem; }
.cash-result.service { border-color: rgba(120,215,120,.3); }
```

- [ ] **Step 6: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 7: Verifica manuale**

Cassa: a schermo NON c'è più la griglia di tutti i prodotti. Scrivendo nella ricerca compaiono prodotti + servizi (se creati in Task 3). Click aggiunge al carrello. Servizio aggiunto come riga `service`.

- [ ] **Step 8: Commit**

```bash
/opt/homebrew/bin/git add app/cash-register.tsx app/globals.css
/opt/homebrew/bin/git commit -m "Cassa: ricerca unificata prodotti+servizi, via la griglia sempre accesa"
```

---

## Task 7: Cliente inline (cerca + crea "Nome Cognome")

**Files:**
- Modify: `app/cash-register.tsx` (componente `CustomerInline`; uso nel render; rimozione bottone "Nuovo cliente")

- [ ] **Step 1: Componente `CustomerInline`**

Aggiungere in `app/cash-register.tsx`:

```tsx
function CustomerInline({ data, store, customer, onSelect, reload }: {
  data: CashData;
  store: Store;
  customer: Customer | null;
  onSelect: (customer: Customer | null) => void;
  reload: () => Promise<void>;
}) {
  const [query, setQuery] = useState("");
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState("");
  const q = query.trim().toLocaleLowerCase("it");
  const matches = q ? data.customers.filter((c) => `${customerLabel(c)} ${c.phone}`.toLocaleLowerCase("it").includes(q)).slice(0, 6) : [];
  const canCreate = q.length >= 2 && !matches.some((c) => customerLabel(c).toLocaleLowerCase("it") === q);
  async function create() {
    setError("");
    if (isClientTestMode?.()) { setError("Modalità TEST: cliente non creato."); return; }
    const tokens = query.trim().split(/\s+/);
    const lastName = tokens.length > 1 ? tokens[tokens.length - 1] : tokens[0];
    const firstName = tokens.length > 1 ? tokens.slice(0, -1).join(" ") : "";
    setBusy(true);
    try {
      const result = await post("createCustomer", { firstName, lastName, store });
      await reload();
      if (result?.customer) onSelect(result.customer as Customer);
      setQuery("");
    } catch (reason) { setError(reason instanceof Error ? reason.message : "Errore."); }
    finally { setBusy(false); }
  }
  if (customer) return <div className="customer-chip"><MaterialIcon>person</MaterialIcon><span>{customerLabel(customer)}</span><button type="button" onClick={() => onSelect(null)} aria-label="Rimuovi cliente"><MaterialIcon>close</MaterialIcon></button></div>;
  return <div className="customer-inline">
    <div className="customer-inline-field"><MaterialIcon>person_search</MaterialIcon><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Associa cliente: Nome Cognome…" /></div>
    {q && <div className="customer-inline-drop">
      {matches.map((c) => <button type="button" key={c.id} onClick={() => { onSelect(c); setQuery(""); }}><strong>{customerLabel(c)}</strong><small>{c.phone || c.city || "—"}</small></button>)}
      {canCreate && <button type="button" className="create" disabled={busy} onClick={create}><MaterialIcon>add</MaterialIcon> Crea "{query.trim()}"</button>}
      {error && <div className="alert danger">{error}</div>}
    </div>}
  </div>;
}
```

> Verificare il nome esatto di `isClientTestMode` in `cash-register.tsx` (se non presente, importarlo/replicarne l'uso come negli altri form; in mancanza, rimuovere il guard perché il server rifiuta comunque in TEST).

- [ ] **Step 2: Usare `CustomerInline` e rimuovere il vecchio pannello cliente + bottone**

Sostituire l'attuale pannello CLIENTE (riga ~654) con un uso compatto di `CustomerInline` nella barra sottile sopra il carrello (vedi Task 9 per la collocazione). Per ora, sostituire il blocco `<div className="panel">…CLIENTE…</div>` con:

```tsx
<CustomerInline data={data} store={store} customer={customer} onSelect={setCustomer} reload={reload} />
```

Nella `actions-strip` (riga ~655) **rimuovere** il primo bottone `action-customer` ("Nuovo cliente"). Rimuovere anche il caso `modal === "customer"` e il componente `CustomerForm` se non più usato altrove (lasciare `CustomerForm` se referenziato in altri punti; altrimenti eliminarlo per pulizia).

- [ ] **Step 3: CSS cliente inline**

In `app/globals.css`:

```css
/* Cassa: cliente inline */
.customer-inline { position: relative; flex: 1; min-width: 220px; }
.customer-inline-field { display: flex; align-items: center; gap: .5rem; padding: .5rem .7rem; border: 1px solid rgba(66,199,255,.25); border-radius: 999px; }
.customer-inline-field input { border: 0; background: transparent; color: inherit; width: 100%; }
.customer-inline-drop { position: absolute; z-index: 20; top: calc(100% + .3rem); left: 0; right: 0; display: flex; flex-direction: column; padding: .35rem; border: 1px solid rgba(66,199,255,.2); border-radius: 12px; background: var(--panel); box-shadow: 0 18px 40px rgba(0,0,0,.42); }
.customer-inline-drop button { display: flex; flex-direction: column; align-items: flex-start; gap: 0; padding: .5rem .6rem; border: 0; border-radius: 8px; background: transparent; color: inherit; text-align: left; cursor: pointer; }
.customer-inline-drop button.create { flex-direction: row; align-items: center; gap: .4rem; font-weight: 700; color: var(--lime, #72d7ff); }
.customer-inline-drop button:hover { background: rgba(66,199,255,.12); }
.customer-inline-drop small { color: var(--muted, #8aa0b5); font-size: .74rem; }
.customer-chip { display: inline-flex; align-items: center; gap: .5rem; padding: .45rem .8rem; border: 1px solid rgba(66,199,255,.3); border-radius: 999px; }
.customer-chip button { border: 0; background: transparent; color: inherit; cursor: pointer; display: grid; place-items: center; }
```

- [ ] **Step 4: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 5: Verifica manuale**

Cassa: digitando un nome nel campo cliente compaiono i match; se non esiste, `Crea "Mario Rossi"` crea (cognome=Rossi, nome=Mario), lo associa e mostra il chip. Il bottone "Nuovo cliente" non c'è più. In TEST non crea.

- [ ] **Step 6: Commit**

```bash
/opt/homebrew/bin/git add app/cash-register.tsx app/globals.css
/opt/homebrew/bin/git commit -m "Cassa: cliente inline (cerca/crea Nome Cognome), via il bottone Nuovo cliente"
```

---

## Task 8: Menu "Operazioni ▾"

**Files:**
- Modify: `app/cash-register.tsx` (componente `OperationsMenu`; uso al posto della `actions-strip`)
- Modify: `app/globals.css` (dropdown operazioni)

- [ ] **Step 1: Componente `OperationsMenu`**

Aggiungere in `app/cash-register.tsx`:

```tsx
function OperationsMenu({ store, onPick }: { store: Store; onPick: (modal: string) => void }) {
  const [open, setOpen] = useState(false);
  const items: [string, string, string][] = [
    ["gift", "redeem", "Buono regalo"],
    ["varie", "shopping_bag", "Varie"],
    ["reservation", "calendar_month", "Prenotazione"],
    ["return", "sync_alt", "Reso / Cambio"],
    store === "Viterbo" ? ["repair", "footprint", "Risuolatura"] : ["shirt", "checkroom", "Maglie Gran Sasso"],
  ];
  return <div className="ops-menu">
    {open && <button className="ops-backdrop" aria-label="Chiudi" onClick={() => setOpen(false)} />}
    <button type="button" className="secondary" onClick={() => setOpen((v) => !v)}><MaterialIcon>bolt</MaterialIcon> Operazioni <MaterialIcon>{open ? "expand_less" : "expand_more"}</MaterialIcon></button>
    {open && <div className="ops-dropdown">{items.map(([key, icon, label]) => <button type="button" key={key} onClick={() => { onPick(key); setOpen(false); }}><MaterialIcon>{icon}</MaterialIcon> {label}</button>)}</div>}
  </div>;
}
```

- [ ] **Step 2: Sostituire la `actions-strip`**

Nel render, sostituire l'intera `<div className="actions-strip">…</div>` (riga ~655, già priva del bottone cliente dopo Task 7) con:

```tsx
<OperationsMenu store={store} onPick={setModal} />
```

Il resto (`{modal && <Modal …>…}`, riga ~666) resta invariato: le chiavi `gift/varie/reservation/return/repair/shirt` combaciano con i modali esistenti.

- [ ] **Step 3: CSS dropdown operazioni**

In `app/globals.css`:

```css
/* Cassa: menu operazioni */
.ops-menu { position: relative; display: inline-flex; }
.ops-backdrop { position: fixed; inset: 0; z-index: 15; border: 0; background: transparent; }
.ops-dropdown { position: absolute; z-index: 16; top: calc(100% + .35rem); right: 0; min-width: 220px; display: flex; flex-direction: column; padding: .4rem; border: 1px solid rgba(66,199,255,.2); border-radius: 12px; background: var(--panel); box-shadow: 0 18px 40px rgba(0,0,0,.42); }
.ops-dropdown button { display: flex; align-items: center; gap: .6rem; padding: .6rem .7rem; border: 0; border-radius: 8px; background: transparent; color: inherit; text-align: left; font-weight: 650; cursor: pointer; }
.ops-dropdown button:hover { background: rgba(66,199,255,.12); }
```

- [ ] **Step 4: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 5: Verifica manuale**

Cassa: al posto della fila di bottoni c'è un solo "Operazioni ▾". Aprendolo si vedono Buono/Varie/Prenotazione/Reso/Risuolatura (o Maglie a Gran Sasso). Ogni voce apre il modale giusto. Si chiude cliccando fuori.

- [ ] **Step 6: Commit**

```bash
/opt/homebrew/bin/git add app/cash-register.tsx app/globals.css
/opt/homebrew/bin/git commit -m "Cassa: operazioni rare raggruppate nel menu Operazioni"
```

---

## Task 9: Ricomposizione layout "carrello protagonista" + responsive

**Files:**
- Modify: `app/cash-register.tsx` (struttura del `return` di `NewCashRegister`)
- Modify: `app/globals.css` (`.cash-grid`, barra scan full-width, barra sottile, sticky checkout)

- [ ] **Step 1: Ristrutturare il markup principale**

Nel `return` di `NewCashRegister`, comporre così (mantenendo header, `notice`, `error`, i modali e `saleDone` invariati):

```tsx
<div className="cash-grid">
  <div className="cash-main">
    <div className="panel scanner-panel">
      <div className="scanner-panel-head"><p className="eyebrow">SCANNER</p><Scanner onScan={scan} /></div>
      <CashSearch products={data.products} services={data.services} available={available} onProduct={addProduct} onService={addService} />
    </div>
    <div className="cash-subbar">
      <CustomerInline data={data} store={store} customer={customer} onSelect={setCustomer} reload={reload} />
      <OperationsMenu store={store} onPick={setModal} />
    </div>
    <div className="panel cart-panel">
      <div className="panel-title"><div><p className="eyebrow">VENDITA</p><h2>Carrello</h2></div><span className="count-pill">{cart.length} righe</span></div>
      {!cart.length ? <Empty>Spara un codice o cerca un prodotto per iniziare.</Empty> : <div className="cart-list">{/* CartRow map dal Task 5 */}</div>}
    </div>
  </div>
  <aside className="checkout">{/* riepilogo + pagamento + Completa (invariati) + last-sale */}</aside>
</div>
```

Spostare il blocco carrello (Task 5) qui, sopra nulla e sotto la barra sottile. Il contenuto di `.checkout` resta quello attuale (riga ~658-663).

- [ ] **Step 2: CSS layout**

In `app/globals.css`, aggiornare/aggiungere:

```css
/* Cassa: layout carrello protagonista */
.cash-grid { display: grid; grid-template-columns: minmax(0, 1fr) 360px; gap: 1.1rem; align-items: start; }
.cash-main { display: flex; flex-direction: column; gap: 1rem; min-width: 0; }
.cash-subbar { display: flex; align-items: center; gap: .8rem; flex-wrap: wrap; }
.checkout { position: sticky; top: 1rem; display: flex; flex-direction: column; gap: 1rem; }
@media (max-width: 900px) { .cash-grid { grid-template-columns: 1fr; } .checkout { position: static; } }
```

> Se `.cash-grid`/`.checkout` esistono già in `globals.css`, sostituire le regole esistenti con queste (cercare le vecchie definizioni e rimpiazzarle per evitare conflitti).

- [ ] **Step 3: Typecheck + build**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori.

- [ ] **Step 4: Verifica manuale (desktop + mobile)**

Desktop: scan in alto, barra `[cliente][Operazioni]`, carrello grande al centro, checkout sticky a destra. Mobile (DevTools ~390px): le colonne si impilano, checkout sotto il carrello, stepper e campi toccabili. Completare una vendita reale end-to-end (contanti con resto) → funziona come prima.

- [ ] **Step 5: Commit**

```bash
/opt/homebrew/bin/git add app/cash-register.tsx app/globals.css
/opt/homebrew/bin/git commit -m "Cassa: layout carrello protagonista + responsive"
```

---

## Task 10: Verifica finale end-to-end

**Files:** nessuna modifica (solo verifica); eventuali fix inline.

- [ ] **Step 1: Typecheck + build puliti**

Run: `npx tsc --noEmit && npx next build`
Expected: 0 errori, warnings preesistenti tollerati.

- [ ] **Step 2: Checklist funzionale (dev)**

`npm run dev`, per admin e per un account negozio:
- Scanner globale: sparo EAN da fuori dal campo → in carrello.
- Carrello: prezzo bloccato, Qtà stepper, Sconto €/% , totale ok, rimozione ok.
- Ricerca: griglia non sempre accesa; prodotti+servizi cercabili; servizio in carrello.
- Cliente inline: cerca/crea Nome Cognome; niente bottone Nuovo cliente.
- Operazioni ▾: tutti i modali (buono/varie/prenotazione/reso/risuolatura|maglie).
- Vendita reale (contanti/carta/misto/buono), resto live, PDF/scontrino: invariati.
- Reso/saldo prenotazione: righe bloccate, importi corretti.
- TEST mode: nessuna scrittura (servizio, cliente, vendita).
- Responsive a 390px: tutto usabile.

- [ ] **Step 3: Commit finale (se fix)**

```bash
/opt/homebrew/bin/git add -A
/opt/homebrew/bin/git commit -m "Cassa: rifiniture redesign dopo verifica end-to-end"
```

---

## Self-review (coverage spec)

- Layout carrello protagonista → Task 9. ✓
- Scanner globale → Task 4. ✓
- Carrello: prezzo bloccato, Qtà+Sconto €/%, totale calcolato, righe speciali bloccate → Task 5. ✓
- Ricerca prodotti+servizi, via griglia sempre accesa → Task 6. ✓
- Cliente inline "Nome Cognome", via bottone Nuovo cliente → Task 7 (+ createCustomer ritorna cliente in Task 2). ✓
- Menu Operazioni → Task 8. ✓
- Catalogo servizi + pagina admin → Task 1 (schema), Task 2 (API), Task 3 (pagina). ✓
- Responsive + logica invariata + build verde → Task 9/10. ✓

Nomi/tipi coerenti tra i task: `services` (bootstrap + `Bootstrap` + `CashData`), `addService`/`CashSearch`/`CartRow`/`CustomerInline`/`OperationsMenu`/`useGlobalScanner`, `euroDiscountToPercent`/`percentToEuroDiscount`. Chiave menu/pagina servizi allineata a `services`.
